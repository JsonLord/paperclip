import { boolean, jsonb, pgTable, text, timestamp, uniqueIndex, uuid } from "drizzle-orm/pg-core";
import { approvals } from "./approvals.js";
import { companies } from "./companies.js";

export const publishingAccounts = pgTable("publishing_accounts", { id: uuid("id").primaryKey().defaultRandom(), companyId: uuid("company_id").notNull().references(() => companies.id, { onDelete: "cascade" }), provider: text("provider").notNull(), providerAccountId: text("provider_account_id").notNull(), platform: text("platform").notNull(), displayName: text("display_name"), capabilities: jsonb("capabilities").$type<string[]>().notNull(), status: text("status").notNull(), lastVerifiedAt: timestamp("last_verified_at", { withTimezone: true }).notNull(), createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow() }, t => ({ providerAccountUnique: uniqueIndex("publishing_accounts_company_provider_account_uniq").on(t.companyId, t.provider, t.providerAccountId) }));
export const contentCandidates = pgTable("content_candidates", { id: uuid("id").primaryKey().defaultRandom(), companyId: uuid("company_id").notNull().references(() => companies.id, { onDelete: "cascade" }), contentId: text("content_id").notNull(), campaignId: text("campaign_id").notNull(), platform: text("platform").notNull(), archetype: text("archetype").notNull(), objective: text("objective").notNull(), audience: text("audience").notNull(), sourceClaimIds: jsonb("source_claim_ids").$type<string[]>().notNull(), evidenceIds: jsonb("evidence_ids").$type<string[]>().notNull(), capabilityIds: jsonb("capability_ids").$type<string[]>().notNull(), copy: jsonb("copy").$type<Record<string, unknown>>().notNull(), media: jsonb("media").$type<Record<string, unknown>[]>().notNull(), claimMaturity: text("claim_maturity").notNull(), approvalId: uuid("approval_id").references(() => approvals.id), approvalStatus: text("approval_status").notNull(), publicationStatus: text("publication_status").notNull(), createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(), updatedAt: timestamp("updated_at", { withTimezone: true }).notNull().defaultNow() }, t => ({ companyContentUnique: uniqueIndex("content_candidates_company_content_uniq").on(t.companyId, t.contentId) }));
export const socialPublications = pgTable("social_publications", { id: uuid("id").primaryKey().defaultRandom(), companyId: uuid("company_id").notNull().references(() => companies.id, { onDelete: "cascade" }), publicationId: text("publication_id").notNull(), contentId: text("content_id").notNull(), campaignId: text("campaign_id").notNull(), provider: text("provider").notNull(), platform: text("platform").notNull(), providerAccountId: text("provider_account_id").notNull(), providerPublicationId: text("provider_publication_id"), status: text("status").notNull(), publicUrl: text("public_url"), failureCode: text("failure_code"), failureSummary: text("failure_summary"), retryEligible: boolean("retry_eligible").notNull().default(false), submittedAt: timestamp("submitted_at", { withTimezone: true }), publishedAt: timestamp("published_at", { withTimezone: true }), createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(), updatedAt: timestamp("updated_at", { withTimezone: true }).notNull().defaultNow() }, t => ({ companyPublicationUnique: uniqueIndex("social_publications_company_publication_uniq").on(t.companyId, t.publicationId) }));
export const contentStrategies = pgTable("content_strategies", { id:uuid("id").primaryKey().defaultRandom(),companyId:uuid("company_id").notNull().references(()=>companies.id,{onDelete:"cascade"}),strategyId:text("strategy_id").notNull(),version:text("version").notNull(),kind:text("kind").notNull(),sourceStateVersion:text("source_state_version").notNull(),contract:jsonb("contract").$type<Record<string,unknown>>().notNull(),status:text("status").notNull(),createdAt:timestamp("created_at",{withTimezone:true}).notNull().defaultNow() },t=>({companyStrategyUnique:uniqueIndex("content_strategies_company_id_version_uniq").on(t.companyId,t.strategyId,t.version)}));
export const gtmCampaigns = pgTable("gtm_campaigns", { id:uuid("id").primaryKey().defaultRandom(),companyId:uuid("company_id").notNull().references(()=>companies.id,{onDelete:"cascade"}),campaignId:text("campaign_id").notNull(),contract:jsonb("contract").$type<Record<string,unknown>>().notNull(),messageMap:jsonb("message_map").$type<Record<string,unknown>>().notNull(),status:text("status").notNull(),decision:text("decision"),createdAt:timestamp("created_at",{withTimezone:true}).notNull().defaultNow(),updatedAt:timestamp("updated_at",{withTimezone:true}).notNull().defaultNow() },t=>({companyCampaignUnique:uniqueIndex("gtm_campaigns_company_campaign_uniq").on(t.companyId,t.campaignId)}));
export const contentAtoms = pgTable("content_atoms", { id:uuid("id").primaryKey().defaultRandom(),companyId:uuid("company_id").notNull().references(()=>companies.id,{onDelete:"cascade"}),atomId:text("atom_id").notNull(),atomType:text("atom_type").notNull(),sourceClaimIds:jsonb("source_claim_ids").$type<string[]>().notNull(),evidenceIds:jsonb("evidence_ids").$type<string[]>().notNull(),classification:text("classification").notNull(),maturity:text("maturity").notNull(),allowedLanguage:text("allowed_language").notNull(),privacyState:text("privacy_state").notNull(),freshness:text("freshness").notNull(),payload:jsonb("payload").$type<Record<string,unknown>>().notNull() },t=>({companyAtomUnique:uniqueIndex("content_atoms_company_atom_uniq").on(t.companyId,t.atomId)}));
export const campaignEvents = pgTable("campaign_events", { id:uuid("id").primaryKey().defaultRandom(),companyId:uuid("company_id").notNull().references(()=>companies.id,{onDelete:"cascade"}),eventId:text("event_id").notNull(),campaignId:text("campaign_id").notNull(),contentId:text("content_id"),channel:text("channel").notNull(),variant:text("variant"),eventType:text("event_type").notNull(),occurredAt:timestamp("occurred_at",{withTimezone:true}).notNull(),source:text("source").notNull(),providerPublicationId:text("provider_publication_id"),subjectRef:text("subject_ref"),qualification:text("qualification").notNull(),attribution:text("attribution").notNull(),metadata:jsonb("metadata").$type<Record<string,unknown>>().notNull() },t=>({companyEventUnique:uniqueIndex("campaign_events_company_event_uniq").on(t.companyId,t.eventId)}));
