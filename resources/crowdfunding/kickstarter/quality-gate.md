# Quality gate

Fail if suitability is assumed; economics do not reconcile; a tier is unsafe; a claim lacks provenance; planned/mockup content is called live; private data lacks authorization; fulfillment stress inputs are missing; platform state is invented; approval is absent; events are unverified; or waiting uses polling.
