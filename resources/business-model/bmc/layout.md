# Layout

Render the canonical nine blocks in any deterministic UI layout. YAML is authoritative for presentation; Markdown is the compact human review. No standalone editor or shadow task state is required.
