import { Router } from "express";
import type { Db } from "@paperclipai/db";
import { pitchService } from "../services/pitch.js";
import { assertBoard, assertCompanyAccess } from "./authz.js";
export function pitchRoutes(db: Db) { const router = Router(), service = pitchService(db); router.post("/companies/:companyId/pitch/activate", async (req, res) => { assertBoard(req); const companyId = req.params.companyId as string; assertCompanyAccess(req, companyId); try { res.status(201).json(await service.activate({ ...req.body, companyId })); } catch (error) { res.status(422).json({ error: error instanceof Error ? error.message : "Pitch activation invalid" }); } }); router.post("/companies/:companyId/pitch/compile", async (req, res) => { assertBoard(req); const companyId = req.params.companyId as string; assertCompanyAccess(req, companyId); try { res.status(201).json(await service.compile({ ...req.body, companyId, compiledAt: new Date(req.body.compiledAt ?? Date.now()) })); } catch (error) { res.status(422).json({ error: error instanceof Error ? error.message : "Pitch invalid" }); } }); return router; }
