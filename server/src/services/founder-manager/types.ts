import type { DeterministicValidationResult } from "../founderos-validation/types.js";
export const FOUNDER_MANAGER_VERDICTS = ["ACCEPT", "REVISE_SAME_SESSION", "RETRY_NEW_SESSION", "WAIT", "ESCALATE", "FAIL_OUTCOME"] as const;
export type FounderManagerVerdict = typeof FOUNDER_MANAGER_VERDICTS[number];
export interface ResultJudgeInput { company: Record<string, unknown>; goal: Record<string, unknown>; project?: Record<string, unknown>; issue: Record<string, unknown>; agent: Record<string, unknown>; objective: string; requiredOutputs: string[]; acceptanceCriteria: string[]; cannotCompleteIf: unknown[]; validation: DeterministicValidationResult; artifacts: unknown[]; evidence: unknown[]; pullRequest?: Record<string, unknown>; completionSummary?: string; attemptHistory: unknown[] }
export interface ResultJudgment { verdict: FounderManagerVerdict; confidence: number; reason: string; passedCriteria: string[]; failedCriteria: string[]; revisionInstructions: string[]; requiresHuman: boolean; recommendedWaitCondition?: string; recommendedNextAction?: string }
export type FailureClass = "INFRASTRUCTURE_TRANSIENT" | "PROVIDER_QUOTA" | "AUTH" | "SOURCE_ACCESS" | "CAPABILITY_MISSING" | "REMOTE_SESSION_FAILURE" | "CI_FAILURE" | "WRITE_SCOPE_VIOLATION" | "SECRET_LEAK" | "QUALITY_FAILURE" | "MISSING_EVIDENCE" | "INVALID_GOAL_CONTRACT" | "EXTERNAL_DEPENDENCY" | "BUSINESS_CONTRADICTION" | "UNKNOWN";
